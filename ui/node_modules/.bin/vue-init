#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../vue-cli/bin/vue-init" "$@"
  ret=$?
else 
  node  "$basedir/../vue-cli/bin/vue-init" "$@"
  ret=$?
fi
exit $ret
